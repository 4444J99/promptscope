{
  "name": "promptscope",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "description": "Free + paid LLM system-prompt analyzer. Cloudflare Pages + Workers AI.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/",
    "typecheck": "tsc --noEmit --target es2022 --lib es2022,webworker --types @cloudflare/workers-types src/index.ts",
    "test": "node test-auth.mjs",
    "kv:create": "scripts/kv-bootstrap.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^6.0.3"
  }
}
